# Store Rating App
Full-stack example: React (frontend) + Express (backend) + MySQL (Sequelize)
Run locally:
- Backend:
  cd backend
  npm install
  create .env (see .env.example)
  npm run dev
- Frontend:
  cd frontend
  npm install
  npm start

This package includes Bootstrap-based responsive UI and sample icons.
